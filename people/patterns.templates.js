// Generated by tools/render.py from university-web-patterns 0.22.0.
// Do not edit, and do not commit edits to it: re-emit from the
// vendored copy after adopting a release.
(function (root) {
  "use strict";
  var templates = {
    "person-card": "<article class=\"{{ #unless person.unframed }}uwp-card {{ /unless }}uwp-person-card{{ #if person.stacked }} uwp-person-card--stacked{{ /if }}\"\n  data-uwp-component=\"person-card\"{{ person_attributes }}>\n  {{ person_portrait }}\n  <div class=\"uwp-person-card__body\">\n    <p class=\"uwp-meta\">{{ #each person.flair }}<span class=\"uwp-person-card__flair\">{{ this }}</span>{{ /each }}{{ person.role }}</p>\n    {{ #if person.url }}\n    <h3><a href=\"{{ person.url }}\">{{ person.display_name }}</a></h3>\n    {{ /if }}\n    {{ #unless person.url }}\n    <h3>{{ person.display_name }}</h3>\n    {{ /unless }}\n    {{ #if person.unit }}\n    <p class=\"uwp-meta\">{{ person.unit }}</p>\n    {{ /if }}\n    {{ person.summary }}\n  </div>\n</article>\n",
    "portrait": "\n{{ #if portrait.href }}<a class=\"uwp-portrait-link\" href=\"{{ portrait.href }}\" tabindex=\"-1\" aria-hidden=\"true\">{{ /if }}\n{{ #if portrait.src }}\n<img class=\"uwp-portrait{{ #if portrait.medium }} uwp-portrait--md{{ /if }}{{ #if portrait.large }} uwp-portrait--lg{{ /if }}\"\n  data-uwp-component=\"portrait\" src=\"{{ portrait.src }}\" alt=\"\"\n  {{ #if portrait.width }}width=\"{{ portrait.width }}\" {{ /if }}{{ #if portrait.height }}height=\"{{ portrait.height }}\" {{ /if }}decoding=\"async\"{{ portrait_loading }}>\n{{ /if }}\n\n{{ #unless portrait.src }}\n<div class=\"uwp-portrait uwp-portrait--blank{{ #if portrait.medium }} uwp-portrait--md{{ /if }}{{ #if portrait.large }} uwp-portrait--lg{{ /if }}\"\n  data-uwp-component=\"portrait\" aria-hidden=\"true\">{{ portrait.initials }}</div>\n{{ /unless }}\n{{ #if portrait.href }}</a>{{ /if }}\n"
  };
  root.uwpTemplates = templates;
  root.uwpVersion = "0.22.0";
  if (root.uwp && typeof root.uwp.register === "function") {
    root.uwp.register(templates);
    root.uwp.registerComponents({version: "0.22.0"});
  }
  if (typeof module === "object" && module && module.exports) {
    module.exports = templates;
  }
})(typeof globalThis !== "undefined" ? globalThis : this);
